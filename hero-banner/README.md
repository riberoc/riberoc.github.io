# Hero Banner

A local Quartz 5 component plugin that displays a note's `cover` frontmatter as a banner and overlays the note title and modified date.

Use an Obsidian-style cover in a note:

```yaml
cover: [[Aboutme_2.png]]
```

The plugin is enabled and placed in `beforeBody` by `quartz.config.yaml`.
